{
  "rules": [
    {
      "allowed_origins": ["*"],
      "allowed_methods": ["GET", "PUT", "HEAD"],
      "allowed_headers": ["*"],
      "expose_headers": ["ETag"],
      "max_age_seconds": 3600
    }
  ]
}
