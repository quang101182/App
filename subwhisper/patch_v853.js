// patch_v853.js — SubWhisper v8.52 → v8.53
// Parallélisation des boucles batch dans translateGemini, cleanAI, translateManual

var fs = require('fs');
var content = fs.readFileSync(__dirname + '/index.html', 'utf8');
var crlf = content.includes('\r\n');
var c = content.replace(/\r\n/g, '\n');
var errors = [];

// ── 1. Version bump ────────────────────────────────────────────────────────────
var old_title = '<title>SubWhisper v8.52</title>';
var new_title = '<title>SubWhisper v8.53</title>';
if (c.indexOf(old_title) === -1) errors.push('Version title v8.52 non trouvé');
else c = c.replace(old_title, new_title);

var old_badge = '<span class="ver-badge">v8.52</span>';
var new_badge = '<span class="ver-badge">v8.53</span>';
if (c.indexOf(old_badge) === -1) errors.push('ver-badge v8.52 non trouvé');
else c = c.replace(old_badge, new_badge);

// ── 2. translateGemini — BATCH adaptatif ──────────────────────────────────────
var old_tg_batch = '  // Découper en batches de 200 blocs pour éviter la limite 8192 tokens de sortie Gemini\n  var BATCH = 200;';
var new_tg_batch = '  // Découper en batches adaptatifs (DeepSeek: 75, Gemini: 200)\n  var BATCH = getAIEngine() === \'deepseek\' ? 75 : 200;';
if (c.indexOf(old_tg_batch) === -1) errors.push('translateGemini BATCH=200 non trouvé');
else c = c.replace(old_tg_batch, new_tg_batch);

// ── 3. translateGemini — boucle séquentielle → parallèle ─────────────────────
var old_tg_loop = '  var results = [];\n  for (var b = 0; b < batches.length; b++) {\n    var label = batches.length > 1 ? \'Traduction \' + engineName + \' \' + (b+1) + \'/\' + batches.length + \'...\' : \'Traduction \' + engineName + \' en cours...\';\n    setLog(label);\n    var batchSrt = batches[b].join(\'\\n\\n\');\n    var typoT = targetLang === \'fr\' ? \'\\n- FRENCH TYPOGRAPHY: Include mandatory space before ? ! : ; in French.\' : \'\';\n    var prompt = \'You are a professional subtitle translator. Translate from \' + srcName + \' to \' + tgtName + \'.\\n1. STRUCTURE: Timestamp (HH:MM:SS,mmm --> HH:MM:SS,mmm) and index lines EXACTLY as-is. NEVER insert timestamp text into subtitle content.\\n2. PROPER NOUNS: Keep character names, place names, invented terms in ORIGINAL form. Capitalized word not starting a sentence = likely proper noun, do not alter.\\n3. COMPLETE TRANSLATION: Translate EVERY line. NEVER output [...] — if uncertain give best translation. Do not skip or merge blocks.\\n4. SONG LYRICS: Text clearly song lyrics in a third language — keep unchanged, do not translate.\\n5. STYLE: Match register and tone. Preserve short exclamations exactly.\\n6. TRUNCATED: Lines ending without punctuation or with ... are intentionally cut — do NOT add words.\' + typoT + \'\\n7. Return ONLY translated SRT, SAME block count, no explanation, no markdown.\\n\\n\' + batchSrt;\n        var text = await callAIText(prompt);\n    results.push(text);\n  }\n  return results.join(\'\\n\\n\') + \'\\n\';';

var new_tg_loop = '  var results = new Array(batches.length);\n  var _tDone = 0;\n  var _tCores = getAIEngine() === \'deepseek\' ? 4 : 1;\n  for (var _tS = 0; _tS < batches.length; _tS += _tCores) {\n    var _tGrp = [];\n    for (var _tK = _tS; _tK < Math.min(_tS + _tCores, batches.length); _tK++) _tGrp.push(_tK);\n    await Promise.all(_tGrp.map(function(b) {\n      return (async function() {\n        var batchSrt = batches[b].join(\'\\n\\n\');\n        var typoT = targetLang === \'fr\' ? \'\\n- FRENCH TYPOGRAPHY: Include mandatory space before ? ! : ; in French.\' : \'\';\n        var prompt = \'You are a professional subtitle translator. Translate from \' + srcName + \' to \' + tgtName + \'.\\n1. STRUCTURE: Timestamp (HH:MM:SS,mmm --> HH:MM:SS,mmm) and index lines EXACTLY as-is. NEVER insert timestamp text into subtitle content.\\n2. PROPER NOUNS: Keep character names, place names, invented terms in ORIGINAL form. Capitalized word not starting a sentence = likely proper noun, do not alter.\\n3. COMPLETE TRANSLATION: Translate EVERY line. NEVER output [...] — if uncertain give best translation. Do not skip or merge blocks.\\n4. SONG LYRICS: Text clearly song lyrics in a third language — keep unchanged, do not translate.\\n5. STYLE: Match register and tone. Preserve short exclamations exactly.\\n6. TRUNCATED: Lines ending without punctuation or with ... are intentionally cut — do NOT add words.\' + typoT + \'\\n7. Return ONLY translated SRT, SAME block count, no explanation, no markdown.\\n\\n\' + batchSrt;\n        var text = await callAIText(prompt);\n        results[b] = text;\n        _tDone++;\n        setLog(\'Traduction \' + engineName + \' \' + _tDone + \'/\' + batches.length + \' \\u2713\');\n      })();\n    }));\n  }\n  return results.join(\'\\n\\n\') + \'\\n\';';

if (c.indexOf(old_tg_loop) === -1) errors.push('translateGemini boucle séquentielle non trouvée');
else c = c.replace(old_tg_loop, new_tg_loop);

// ── 4. cleanAI — BATCH adaptatif ──────────────────────────────────────────────
var old_ca_batch = '  var BATCH = 200;\n  var btn = document.getElementById(\'cleanAIBtn\');';
var new_ca_batch = '  var BATCH = getAIEngine() === \'deepseek\' ? 75 : 200;\n  var btn = document.getElementById(\'cleanAIBtn\');';
if (c.indexOf(old_ca_batch) === -1) errors.push('cleanAI BATCH=200 non trouvé');
else c = c.replace(old_ca_batch, new_ca_batch);

// ── 5. cleanAI — boucle séquentielle → parallèle ─────────────────────────────
var old_ca_loop = '    for (var b = 0; b < batches.length; b++) {\n      var label = batches.length > 1 ? \'✨ Nettoyage \' + engineName + \' \' + (b+1) + \'/\' + batches.length + \'...\' : \'✨ Nettoyage \' + engineName + \'...\';\n      toastPersist(label);\n      if (btn) btn.innerHTML = batches.length > 1 ? \'✨ IA \' + (b+1) + \'/\' + batches.length : \'✨ IA...\';\n      var batchBlocks = batches[b];\n      var batchOffset = b * BATCH;\n      var isCJK_c = /^(zh|ja|ko)$/.test(srtTextLang);\n      var langLine = srtTextLang ? \'The subtitle text language is "\' + srtTextLang + \'". \' : \'\';\n      var typoRule = srtTextLang === \'fr\'\n        ? \'\\n5. FRENCH: Fix missing apostrophes (c est->c\\\'est, j ai->j\\\'ai, qu il->qu\\\'il, s il->s\\\'il, etc.). Add space before ? ! : ; if missing.\'\n        : (isCJK_c ? \'\\n5. CJK: Fix wrong characters with similar pronunciation only. Do NOT convert Traditional/Simplified.\' : \'\');\n      var bracketsRule = isCJK_c\n        ? \'\\n4. Incoherent sequences = hallucination — replace inline with [...] only if partial. NEVER entire line.\'\n        : \'\\n4. Foreign words MAY be intentional. Only [...] for partial unintelligible noise inline. NEVER entire line.\';\n      // Construire lignes numérotées — texte multiligne joint avec \' | \'\n      var promptLines = batchBlocks.map(function(blk, j) {\n        return \'[\' + (batchOffset + j + 1) + \'] \' + blk.text.replace(/\\n/g, \' | \');\n      });\n      var prompt = \'You are a professional subtitle editor. \' + langLine + \'\\n\' +\n        \'Each line is [N] subtitle_text. Return EACH line as [N] corrected_text.\\n\' +\n        \'1. NEVER skip a number. If text is already correct, return it UNCHANGED.\\n\' +\n        \'2. PROPER NOUNS: Never alter character names, place names, invented terms. Capitalized word not starting a sentence = likely proper noun.\\n\' +\n        \'3. INTERJECTIONS & TRUNCATED: Short exclamations (Hé, Oh, Ha, Eï, Ouh, Bah, Tss, Yeah, Kiii, etc.) are VALID — NEVER replace. Lines ending without punctuation are intentionally cut — never add words.\\n\' +\n        bracketsRule + typoRule + \'\\n\' +\n        \'6. CORRECTIONS: Fix spelling errors, missing apostrophes, obvious Whisper mishearing only.\\n\' +\n        \'Return ONLY the numbered lines [N] text. No SRT structure, no timestamps, no explanation.\\n\\n\' +\n        promptLines.join(\'\\n\');\n      try {\n        var aiText = await callAIText(prompt);\n        // Parser les marqueurs [N] dans la réponse IA\n        var lineRx = /^\\[(\\d+)\\]\\s*(.*)/gm;\n        var m;\n        while ((m = lineRx.exec(aiText)) !== null) {\n          var idx = parseInt(m[1]) - 1;\n          if (idx >= 0 && idx < origBlocks.length) {\n            var corrected = m[2].replace(/ \\| /g, \'\\n\').trim();\n            if (corrected) correctedTexts[idx] = corrected;\n          }\n        }\n      } catch(e2) {\n        console.warn(\'Batch \' + b + \' erreur (textes originaux conservés):\', e2.message);\n      }\n    }';

var new_ca_loop = '    var _cDone = 0;\n    var _cCores = getAIEngine() === \'deepseek\' ? 4 : 1;\n    for (var _cS = 0; _cS < batches.length; _cS += _cCores) {\n      var _cGrp = [];\n      for (var _cK = _cS; _cK < Math.min(_cS + _cCores, batches.length); _cK++) _cGrp.push(_cK);\n      await Promise.all(_cGrp.map(function(b) {\n        return (async function() {\n          var batchBlocks = batches[b];\n          var batchOffset = b * BATCH;\n          var isCJK_c = /^(zh|ja|ko)$/.test(srtTextLang);\n          var langLine = srtTextLang ? \'The subtitle text language is "\' + srtTextLang + \'". \' : \'\';\n          var typoRule = srtTextLang === \'fr\'\n            ? \'\\n5. FRENCH: Fix missing apostrophes (c est->c\\\'est, j ai->j\\\'ai, qu il->qu\\\'il, s il->s\\\'il, etc.). Add space before ? ! : ; if missing.\'\n            : (isCJK_c ? \'\\n5. CJK: Fix wrong characters with similar pronunciation only. Do NOT convert Traditional/Simplified.\' : \'\');\n          var bracketsRule = isCJK_c\n            ? \'\\n4. Incoherent sequences = hallucination — replace inline with [...] only if partial. NEVER entire line.\'\n            : \'\\n4. Foreign words MAY be intentional. Only [...] for partial unintelligible noise inline. NEVER entire line.\';\n          var promptLines = batchBlocks.map(function(blk, j) {\n            return \'[\' + (batchOffset + j + 1) + \'] \' + blk.text.replace(/\\n/g, \' | \');\n          });\n          var prompt = \'You are a professional subtitle editor. \' + langLine + \'\\n\' +\n            \'Each line is [N] subtitle_text. Return EACH line as [N] corrected_text.\\n\' +\n            \'1. NEVER skip a number. If text is already correct, return it UNCHANGED.\\n\' +\n            \'2. PROPER NOUNS: Never alter character names, place names, invented terms. Capitalized word not starting a sentence = likely proper noun.\\n\' +\n            \'3. INTERJECTIONS & TRUNCATED: Short exclamations (Hé, Oh, Ha, Eï, Ouh, Bah, Tss, Yeah, Kiii, etc.) are VALID — NEVER replace. Lines ending without punctuation are intentionally cut — never add words.\\n\' +\n            bracketsRule + typoRule + \'\\n\' +\n            \'6. CORRECTIONS: Fix spelling errors, missing apostrophes, obvious Whisper mishearing only.\\n\' +\n            \'Return ONLY the numbered lines [N] text. No SRT structure, no timestamps, no explanation.\\n\\n\' +\n            promptLines.join(\'\\n\');\n          try {\n            var aiText = await callAIText(prompt);\n            var lineRx = /^\\[(\\d+)\\]\\s*(.*)/gm;\n            var m;\n            while ((m = lineRx.exec(aiText)) !== null) {\n              var idx = parseInt(m[1]) - 1;\n              if (idx >= 0 && idx < origBlocks.length) {\n                var corrected = m[2].replace(/ \\| /g, \'\\n\').trim();\n                if (corrected) correctedTexts[idx] = corrected;\n              }\n            }\n            _cDone++;\n            toastPersist(\'✨ Nettoyage \' + engineName + \' \' + _cDone + \'/\' + batches.length + \' \\u2713\');\n            if (btn) btn.innerHTML = \'✨ IA \' + _cDone + \'/\' + batches.length;\n          } catch(e2) {\n            console.warn(\'Batch \' + b + \' erreur (textes originaux conservés):\', e2.message);\n          }\n        })();\n      }));\n    }';

if (c.indexOf(old_ca_loop) === -1) errors.push('cleanAI boucle séquentielle non trouvée');
else c = c.replace(old_ca_loop, new_ca_loop);

// ── 6. translateManual mode FR — BATCH adaptatif ─────────────────────────────
var old_tm_batch = '      var BATCH = 200;\n      var totalBatches = Math.ceil(blocks.length / BATCH);';
var new_tm_batch = '      var BATCH = getAIEngine() === \'deepseek\' ? 75 : 200;\n      var totalBatches = Math.ceil(blocks.length / BATCH);';
if (c.indexOf(old_tm_batch) === -1) errors.push('translateManual BATCH=200 non trouvé');
else c = c.replace(old_tm_batch, new_tm_batch);

// ── 7. translateManual mode FR — boucle séquentielle → parallèle ─────────────
var old_tm_loop = '      for (var b = 0; b < totalBatches; b++) {\n        var batchBlocks = blocks.slice(b * BATCH, (b+1) * BATCH);\n        var batchOffset = b * BATCH;\n        var bLabel = totalBatches > 1\n          ? \'🌐 Patch FR \' + engine + \' \' + (b+1) + \'/\' + totalBatches + \'...\'\n          : \'🌐 Patch FR \' + engine + \'...\';\n        toastPersist(bLabel);\n        if (btn) btn.innerHTML = totalBatches > 1 ? \'🌐 \' + (b+1) + \'/\' + totalBatches : \'🌐 ...\';\n        var promptLines = batchBlocks.map(function(blk, j) {\n          return \'[\' + (batchOffset + j + 1) + \'] \' + blk.text.replace(/\\n/g, \' | \');\n        });\n        var prompt = \'You are a professional subtitle translator. Target language: French.\\n\' +\n          \'Each line is [N] subtitle_text.\\n\' +\n          \'1. If the text is ALREADY in French: return it UNCHANGED as [N] text.\\n\' +\n          \'2. If the text is in ANY OTHER language: translate it to French as [N] French_text.\\n\' +\n          \'3. NEVER skip a number. Return ALL lines.\\n\' +\n          \'4. PROPER NOUNS: Keep character names, place names in their original form.\\n\' +\n          \'5. TRUNCATED: Lines ending without punctuation are intentionally cut — never add words.\\n\' +\n          \'6. FRENCH TYPOGRAPHY: Add mandatory space before ? ! : ; if missing.\\n\' +\n          \'Return ONLY the numbered lines [N] text. No SRT structure, no timestamps, no explanation.\\n\\n\' +\n          promptLines.join(\'\\n\');\n        try {\n          var aiText = await callAIText(prompt);\n          var lineRx = /^\\[(\\d+)\\]\\s*(.*)/gm;\n          var m;\n          while ((m = lineRx.exec(aiText)) !== null) {\n            var idx = parseInt(m[1]) - 1;\n            if (idx >= 0 && idx < blocks.length) {\n              var corrected = m[2].replace(/ \\| /g, \'\\n\').trim();\n              if (corrected) correctedTexts[idx] = corrected;\n            }\n          }\n        } catch(e2) {\n          console.warn(\'Batch \' + b + \' erreur:\', e2.message);\n        }\n        if (b < totalBatches - 1) await new Promise(function(r) { setTimeout(r, 500); });\n      }';

var new_tm_loop = '      var _pDone = 0;\n      var _pCores = getAIEngine() === \'deepseek\' ? 4 : 1;\n      for (var _pS = 0; _pS < totalBatches; _pS += _pCores) {\n        var _pGrp = [];\n        for (var _pK = _pS; _pK < Math.min(_pS + _pCores, totalBatches); _pK++) _pGrp.push(_pK);\n        await Promise.all(_pGrp.map(function(b) {\n          return (async function() {\n            var batchBlocks = blocks.slice(b * BATCH, (b+1) * BATCH);\n            var batchOffset = b * BATCH;\n            var promptLines = batchBlocks.map(function(blk, j) {\n              return \'[\' + (batchOffset + j + 1) + \'] \' + blk.text.replace(/\\n/g, \' | \');\n            });\n            var prompt = \'You are a professional subtitle translator. Target language: French.\\n\' +\n              \'Each line is [N] subtitle_text.\\n\' +\n              \'1. If the text is ALREADY in French: return it UNCHANGED as [N] text.\\n\' +\n              \'2. If the text is in ANY OTHER language: translate it to French as [N] French_text.\\n\' +\n              \'3. NEVER skip a number. Return ALL lines.\\n\' +\n              \'4. PROPER NOUNS: Keep character names, place names in their original form.\\n\' +\n              \'5. TRUNCATED: Lines ending without punctuation are intentionally cut — never add words.\\n\' +\n              \'6. FRENCH TYPOGRAPHY: Add mandatory space before ? ! : ; if missing.\\n\' +\n              \'Return ONLY the numbered lines [N] text. No SRT structure, no timestamps, no explanation.\\n\\n\' +\n              promptLines.join(\'\\n\');\n            try {\n              var aiText = await callAIText(prompt);\n              var lineRx = /^\\[(\\d+)\\]\\s*(.*)/gm;\n              var m;\n              while ((m = lineRx.exec(aiText)) !== null) {\n                var idx = parseInt(m[1]) - 1;\n                if (idx >= 0 && idx < blocks.length) {\n                  var corrected = m[2].replace(/ \\| /g, \'\\n\').trim();\n                  if (corrected) correctedTexts[idx] = corrected;\n                }\n              }\n              _pDone++;\n              toastPersist(\'🌐 Patch FR \' + engine + \' \' + _pDone + \'/\' + totalBatches + \' \\u2713\');\n              if (btn) btn.innerHTML = \'🌐 \' + _pDone + \'/\' + totalBatches;\n            } catch(e2) {\n              console.warn(\'Batch \' + b + \' erreur:\', e2.message);\n            }\n          })();\n        }));\n      }';

if (c.indexOf(old_tm_loop) === -1) errors.push('translateManual boucle séquentielle FR non trouvée');
else c = c.replace(old_tm_loop, new_tm_loop);

// ── Vérification et écriture ──────────────────────────────────────────────────
if (errors.length > 0) {
  console.error('ERREURS:', errors);
  process.exit(1);
}

if (crlf) c = c.replace(/\n/g, '\r\n');
fs.writeFileSync(__dirname + '/index.html', c);
console.log('OK - patch v8.53 appliqué (6 remplacements: version x2, translateGemini x2, cleanAI x2, translateManual x2)');
