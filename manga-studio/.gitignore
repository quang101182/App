# checkpoints LoRA : 218 Mo piece, se rejouent depuis dataset/ (cf README)
*.safetensors
*.pt
*.pth
# sorties de test
out/
output/
ingest_out/
*_out/

# Visualisations produites par panel_yolo.py sur les pages de Quang.
# Ses pages ne sont PAS versionnees (README) -- et un JPEG annote en est une.
yolo_*.jpg
scripts/yolo_*.jpg
scripts/ingest_out/
scripts/models/

__pycache__/
*.pyc

# Datasets PRODUITS par la boucle de validation et arborescence d'entrainement :
# ce sont des artefacts, ils se regenerent depuis les cases validees. Seul
# dataset/ (le dataset d'origine du LoRA v1) est versionne -- sans lui, aucun
# chiffre de la roadmap ne serait reproductible.
dataset_*/
scripts/lora_train/

# Sorties REGENERABLES des scripts. Regle generale : si un script le recree a
# l'identique, ca ne se versionne pas -- seul le script se versionne.
scripts/poses/
scripts/pose_out/
scripts/compare_out/
scripts/manga_out/
scripts/scene_out/

# sorties du banc de mouvement (regenerables)
scripts/mouvement_out/
