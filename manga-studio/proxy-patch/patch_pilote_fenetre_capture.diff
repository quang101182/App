9813a9814,9817
> # 26/09/2026 : gestes de fenetre autorises PENDANT une capture (mesures 25/09, scripts/test_deplacer_fenetre.py)
> PILOTE_OK_EN_CAPTURE = ("fenetre_taille", "fenetre_memoriser", "fenetre_etat", "fenetre_ranger")
> 
> 
9816c9820,9821
<     if _FETCH["proc"] is not None and _FETCH["proc"].poll() is None:
---
>     en_capture = _FETCH["proc"] is not None and _FETCH["proc"].poll() is None
>     if en_capture and action not in PILOTE_OK_EN_CAPTURE:
9817a9823,9824
>     if en_capture:
>         data = dict(data, en_capture=True)                     # cdp_mini : « ranger » verifie la taille, « fermer » refuse
